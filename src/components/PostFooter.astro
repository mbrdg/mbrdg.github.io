---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<section class="border-t border-zinc-800">
  <menu class="not-prose flex items-center space-x-4">
    {tags.map(tag => <li><a href=`/tags/${tag}`>{tag}</a></li>)}
  </menu>
</section>
